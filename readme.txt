How to start a project
  1. on proj folder open cmd
    1.1. npm install - load from npm all packages from package.json file (devDep)
    1.2. webpack (start it for dev porposes) - no need for backend devs
    1.3. node dev-server
    1.4. go to http://localhost:3005/#/main